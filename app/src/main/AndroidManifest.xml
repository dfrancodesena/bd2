<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.ufv.daniel.medcap">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListadeMedicosActivity">

        </activity>
        <activity
            android:name=".CadastrarEspecialidadeActivity"
            android:label="Cadastro de Especialidade" />
        <activity
            android:name=".CadastroMedicoActivity"
            android:label="Cadastro de Médico" />
        <activity
            android:name=".CadastroEnderecoActivity"
            android:label="Cadastro de Endereço" />
        <activity android:name=".ListadeEspecialidadesActivity" />
        <activity
            android:name=".CadastrarTipoEspecialidadeActivity"
            android:label=".Novo tipo de Especialidade" />
        <activity android:name=".MainActivity"
            > <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
    </application>

</manifest>